import 'package:flutter/material.dart';

// Aquí pondremos los colores de tu Figma
class AppColors {
  // Evita que alguien instancie esta clase
  AppColors._();

  // Ejemplo basado en tu diseño (¡reemplaza el 0xFFFF0000 con el valor real!)
  static const Color primaryRed = Color(0xFFD90429); // Estimación del rojo de tu botón
  static const Color primaryText = Color(0xFF2B2B2B);
  static const Color secondaryText = Color(0xFF757575);
}